<html>
    <head>
        <title>
            Todo List
        </title>
        <link rel="stylesheet" href="css/styles.css">
    </head>

    <body>
        <div class="box">
            <h1>To-Do List</h1>
            <div class="box">

            <% dayej.forEach(function(items){ %>
                <form action="/delete" method="post">
                    <div class="item">
                        <input type="checkbox" name="checkbox1" value="<%= items.id%>" onchange="this.form.submit()">    
                        <p><%=items.name%></p>
                       </div>
                </form>
                
            <% }) %>
               
                
            <% }%>
                <form action="/" method="POST">
                    <input type="text" name="ele1" placeholder="Enter the text">
                    <button type="submit">+</button>
                </form>
            </div>
        </div>
    </body>
</html>